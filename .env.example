# The Ethereum JSON-RPC endpoint the verifier fetches from (required).
RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY

# Where the verifier listens (optional, default 127.0.0.1:3000).
#BIND_ADDR=127.0.0.1:3000
